Compliance Audit

Reviewed backend-cleaning controllers, services, and Prisma schema to map the requested compliance pillars, capture proof points, and highlight missing automation before prepping the matrix/map/TODOs below.
Compliance Matrix
| Item de compliance | Status | Evidência | Risco se ausente | Ação recomendada |
| Blindagem contra vínculo trabalhista (recorrência/habitualidade) | PASS | BookingsService.ensureWeeklyLimit + runWithWeeklyLock prevent >2 confirmados por semana antes do booking.create e de transições feitas por providers (bookings.service.ts (lines 366-441),  (lines 882-925),  (lines 2197-2232)). | Excesso de bookings com o mesmo prestador poderia caracterizar vínculo trabalhista e quebrar a regra internamente. | Monitorar contadores semanais e ajustar limite/escopo quando a política mudar. |
| KYC / identidade / validação de prestador (documento + selfie + status) | PASS | Provider model guarda documentos/selfie e verificationStatus, registerProvider marca novos providers como PENDING_INITIAL_REVIEW, VerificationService exige uploads + manual review + rejeições, e BookingsService.create só aceita providers APPROVED (schema.prisma (lines 418-468), auth.service.ts (lines 357-430), verification.service.ts (lines 31-205), bookings.service.ts (lines 742-781)). | Sem esse controle, provedores sem validação poderiam aceitar jobs, elevando risco legal e fraudulento. | Continuar enriquecendo painéis de revisão manual (ex.: ProvidersService.getPendingProviders já filtra statuses pendentes). |
| Evidências operacionais (StartJob/EndJob, fotos, GPS, timestamps) | PARTIAL | arriveAtLocation/startService/completeService guardam arrivedAt/startedAt/completedAt, exigem prova obrigatória para seguros premium e lançam notificações (bookings.service.ts (lines 2585-2745)). BookingProof tabela + DTOs armazenam fotos + timestamps/hashes e há endpoints de check-in/check-out (schema.prisma (lines 606-642), booking-proof.dto.ts, bookings.controller.ts (lines 191-233)). | Sem GPS dinâmico, ainda falta comprovar o local real do serviço, reduzindo a força das evidências em auditorias. | Expandir coleta de localização (ex.: BookingProof.timestamps + novos campos lat/lng) e garantir logs de GPS nos eventos de início/fim. |
| Seguro / claims / auditoria | PASS | INSURANCE_PLANS definem cobertura/proofRequired, InsuranceService regula elegibilidade, booking.create persiste bookingInsurance e IncidentsService cria sinistros com janela de 24h, verificação de provas e dedutíveis (insurance.constants.ts, insurance.service.ts, bookings.service.ts (lines 905-980), incidents.service.ts (lines 11-75), schema.prisma (lines 591-1030)). | Sem esses blocos, seguro não teria regras para provar/recusar sinistros nem rastrear valores dedutíveis. | Automatizar notificações de calendário de sinistros e registrar snapshots extras (ex.: estado do booking + provas anexas). |
| Rastro financeiro e antifraude (PIX, ledger, idempotência, disputes) | PASS | PaymentIntentLocker garante um único intent por booking; createPixCharge usa idempotency key e grava PaymentIntent com QR/PagBank (payment-intent-locker.ts (lines 1-37), payments.service.ts (lines 1020-1162)); handlePaymentWebhook valida HMAC, atualiza estados, cria ledger HOLD/FEE e dispara notif. O BookingsService.updateStatus e DisputeService completam ledger EARNING/HOLD/RELEASE/REFUND, e os controladores de disputa aceitam Idempotency-Key (payments.service.ts (lines 210-370),  (lines 1260-1340), bookings.service.ts (lines 2280-2414), dispute.service.ts (lines 49-360), dispute.controller.ts (lines 55-133)). | Sem ledger ou idempotência, webhooks dupicados podem gerar repasses incorretos, dificultando auditoria e maximizando fraudes. | Registrar Webhook events em PaymentEvent (hoje implícito) e adicionar alertas para discrepâncias de ledger/PIX. |
| Desintermediação (proteções, incentivos, políticas) | PARTIAL | ChatService só permite comunicação se houver booking confirmado e bloqueia após cancelamento/finalização, DisputeService gera tickets automatizados e notifica admins/partes, IncidentsService aceita sinistros somente dentro da janela de 24h e exige provas (chat.service.ts (lines 64-210), dispute.service.ts (lines 214-360), incidents.service.ts (lines 14-75)). | Sem moderação de conteúdo e bloqueio explícito de contatos externos, clientes/providers ainda conseguem contatar-se fora da plataforma. | Complementar com análise de conteúdo (regex de telefone/link) e penalidades registradas quando detectado contato fora do app. |
| Contratos e políticas operacionais refletidas em regras (não só textos) | PARTIAL | Há UserConsent no Prisma e o ComplianceService registra consentimentos, checa versões, entrega DSARs e trata erasure (schema.prisma (lines 862-874), compliance.service.ts (lines 23-309)). | Sem expor esse serviço ao front ou acioná-lo, não há registro real de termos aceitos nem trilha de LGPD. | Criar controller/endpoint para registrar consentimentos e integrar ComplianceService.recordConsent aos fluxos de onboarding; logar cada alteração. |

Implementation Map

Blindagem contra vínculo trabalhista: Models/DB = Booking com scheduledStart, status e índices (schema.prisma (lines 523-620)); Endpoints = POST /bookings + status updates via BookingsController/BookingsService; Rules = ensureWeeklyLimit + runWithWeeklyLock invoked before persistence/confirmation; Logs/Auditoria = [BookingsService] weekly limit violation warnings and telemetry booking_status_updated.
KYC / identidade: Models/DB = Provider carries document/selfie URLs + verificationStatus + enums (verification-status.enum.ts); Endpoints = registerProvider + VerificationController actions; Rules = registerProvider seeds PENDING_INITIAL_REVIEW, VerificationService updates statuses after uploads, manual approvals add rejection reasons, BookingsService refuses non-APPROVED providers; Logs/Auditoria = log keys in VerificationService/BookingsService and ProvidersService.getPendingProviders.
Evidências operacionais: Models/DB = Booking timestamps, BookingProof (photos/timestamps/hashes), Address lat/lng; Endpoints = /bookings/:id/proof/{checkin,checkout}, arrive/start/complete flows; Rules = arriveAtLocation, startService (proof/payment window), completeService (checkout proof video, payment, expected end), BookingProof creation with hash/timestamp storage; Logs/Auditoria = logs on proof creation and scheduler notifications for start/end.
Seguro / claims: Models/DB = BookingInsurance, InsuranceClaim, InsurancePlanId enum; Endpoints = insurance plans controller + /incidents (create/get); Rules = InsuranceService evaluates plan eligibility, booking creation attaches insurance, IncidentsService.createClaim enforces 24h window, proof requirements, deductible logic; Logs/Auditoria = log statements describing claim status and automatic UserRole checks.
Rastro financeiro / antifraude: Models/DB = PaymentIntent, LedgerEntry, Payout references; Endpoints = payments routes + disputes endpoints; Rules = PaymentIntentLocker + createPixCharge idempotency, handlePaymentWebhook HMAC/ledger, updateStatus ledger EARNING/HOLD/RELEASE/REFUND, DisputeService holds/releases, DisputeController accepts Idempotency-Key; Logs/Auditoria = Payments webhook logs warning on duplicate intent, ledger warnings in disputes, notifications enqueued with idempotency keys.
Desintermediação: Models/DB = Chat + Booking statuses; Endpoints = ChatService use in /chat controller + /disputes; Rules = chat gating to confirmed bookings, DisputeService auto-creates tickets/messages and notifies admins, IncidentsService forces claims to go through platform; Logs/Auditoria = chat logs referencing blocked access, dispute logs referencing notifications.
Contratos/políticas: Models/DB = UserConsent; Endpoints = TBD (service not yet exposed); Rules = ComplianceService.recordConsent/checkConsent, DSAR, erasure flows; Logs/Auditoria = service logs consent/DSAR/erasure actions.
Hardening TODOs

P0: Capture GPS at start/finish (e.g., add startedLat/startedLng/completedLat/completedLng and persist values from the client together with BookingProof.timestamps) so operational evidence includes location, aligning with compliance requirement 3. Implement server validation that GPS exists when proof is mandatory.
P1: Expose ComplianceService via controller/middleware and call recordConsent during onboarding (client/provider registration) so actual consent versions and DSAR/erasure requests become traceable instead of future code coverage only; add audit log for every consent change.
P2: Augment anti‑desintermediação by scanning chat/dispute messages for contact strings (phone/email/links) and recording sanitized hits with penalties; integrate alerts/metrics so compliance can prove platform mediation at the content level.