# Aprenda mais: https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# Dependências
node_modules/
yarn-error.log
npm-debug.log*
yarn-debug.log*

# Dependências específicas das Cloud Functions (se estiverem dentro da pasta functions)
functions/node_modules/

# Saídas de Build e Caches do Expo / Metro
.expo/
dist/
web-build/
.metro-health-check*

# Saída de Transpilação TypeScript para Cloud Functions
# Assumindo que a sua tsconfig.json em 'functions' tem "outDir": "lib"
functions/lib/

# Arquivos de Build Nativos e Chaves (se gerados localmente e não geridos pelo EAS)
# Android
android/app/build/
android/.gradle/
# android/app/src/main/assets/app.manifest # Gerado pelo EAS Metadata, geralmente não precisa ignorar se o android/ não está no git
*.jks
*.keystore
*.key # Chaves genéricas

# iOS
ios/build/
ios/Pods/
*.p8
*.p12
*.mobileprovision

# Outros nativos
.kotlin/
*.orig

# Arquivos de Ambiente Locais (NUNCA commite arquivos .env com segredos)
.env
.env.*
!.env.example
# A linha acima garante que .env.example SEJA commitado, se você usar esse padrão.

# Ficheiros do Firebase CLI e Emuladores
.firebase/
firebase-debug.log
firestore-debug.log
database-debug.log
ui-debug.log
storage-debug.log
*.emulators.export
emulators-data/ # Ou o diretório configurado para os dados dos emuladores via firebase.json

# Arquivos do Editor e Sistema Operacional
# VS Code (ignora a pasta, mas permite settings e extensions compartilhados)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json

# macOS
.DS_Store
# Windows
Thumbs.db
ehthumbs.db

# TypeScript
*.tsbuildinfo
# *.js # Descomente se você transpila TS para JS no mesmo diretório e só quer commitar TS
# *.js.map # Descomente se não quiser commitar source maps

# Arquivos de Log diversos
*.log
logs/
*.log.*

# Cobertura de Testes
coverage/
lcov.info

# Certificados e chaves PEM (se não forem parte essencial do repo e não sensíveis)
*.pem

# Outros binários de build (APK, IPA, AAB são geralmente gerados por CIs ou EAS)
# Se você construir localmente e não quiser rastreá-los:
# *.apk
# *.ipa
# *.aab

# Ficheiros específicos do EAS Build (geralmente não são commitados se gerados localmente)
eas-build-local-credentials/
*.tar.gz # Builds locais do EAS

# Se 'app-example' for uma pasta específica sua que não deve ser commitada:
# app-example/

# A seção gerada pelo expo-cli abaixo foi modificada para NÃO ignorar expo-env.d.ts
# Se o expo-cli adicionar 'expo-env.d.ts' de volta aqui, você pode removê-lo manualmente.
# ------------------------------------------------------------------------------------
# @generated expo-cli sync-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# The following patterns were generated by expo-cli

# expo-env.d.ts <-- MANTIDO FORA DESTA SECÇÃO PARA SER COMMITADO

# @end expo-cli
# ------------------------------------------------------------------------------------

# Ficheiros de configuração de IDEs
.idea/

# Ficheiros de cache do Jest
.jest-cache

# Ficheiros de build do Yarn PnP (se usado)
.pnp.*
.yarn/*