# Aprenda mais: https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# Dependências
node_modules/
yarn-error.log
npm-debug.log*
yarn-debug.log*

# Saídas de Build e Caches do Expo / Metro
.expo/
dist/
web-build/
.metro-health-check*

# Arquivos de Build Nativos e Chaves (se gerados localmente)
# Android
android/app/build/
android/.gradle/
# android/app/src/main/assets/app.manifest # Gerado pelo EAS Metadata, geralmente não precisa ignorar se o android/ não está no git
*.jks
*.keystore
*.key # Chaves genéricas
# iOS
ios/build/
ios/Pods/
*.p8
*.p12
*.mobileprovision
# Outros nativos
.kotlin/
*.orig

# Arquivos de Ambiente Locais (NUNCA commite arquivos .env com segredos)
.env
.env.*
!.env.example
# A linha acima garante que .env.example SEJA commitado, se você usar esse padrão.
# A linha .env.* já cobre .env.local, .env.development, etc.

# Arquivos do Editor e Sistema Operacional
# VS Code (ignora a pasta, mas permite settings e extensions compartilhados)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
# macOS
.DS_Store
# Windows
Thumbs.db
ehthumbs.db

# TypeScript
*.tsbuildinfo
# *.js # Descomente se você transpila TS para JS no mesmo diretório e só quer commitar TS
# *.js.map # Descomente se não quiser commitar source maps

# Arquivos de Log diversos
*.log
logs/ # Adicionando a pasta logs também
*.log.*

# Cobertura de Testes
coverage/

# Certificados e chaves PEM (se não forem parte essencial do repo e não sensíveis)
*.pem # Já estava na sua lista, bom manter.

# Outros binários de build
*.apk
*.ipa
*.aab

# Se 'app-example' for uma pasta específica sua que não deve ser commitada:
# app-example/

# A seção gerada pelo expo-cli abaixo foi modificada para NÃO ignorar expo-env.d.ts
# Se o expo-cli adicionar 'expo-env.d.ts' de volta aqui, você pode removê-lo manualmente.
# ------------------------------------------------------------------------------------
# @generated expo-cli sync-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# The following patterns were generated by expo-cli

# expo-env.d.ts <-- REMOVIDO DESTA SEÇÃO PARA SER COMMITADO

# @end expo-cli
# ------------------------------------------------------------------------------------